# Déploiement Netlify (CONTEXT.md §7)
[build]
  command = "npm run build"
  publish = "dist"

# App web unique (SPA) : toutes les routes renvoient vers index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
